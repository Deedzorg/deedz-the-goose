{
  "name": "deedz-the-goose-online",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deedz the Goose Online — browser game with a persistent PostgreSQL leaderboard and live player presence.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check src/main.js && node --check src/online.js",
    "test": "npm run check"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "ws": "^8.18.3"
  }
}
