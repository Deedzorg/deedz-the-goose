{
  "name": "deedz-the-goose-online",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Deedz the Goose Online — live flock multiplayer browser platformer with PostgreSQL-backed community stats.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check src/main.js && node --check src/online.js && node --check src/controls.js && node --check src/entities.js && node --check src/render.js && node --check src/levels.js",
    "test": "npm run check"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "ws": "^8.18.3"
  }
}
